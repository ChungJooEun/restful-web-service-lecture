server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG

spring:
  message:
     basename: messages
  security:
    user:
      name: username
      password: p@ssw0rd
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

